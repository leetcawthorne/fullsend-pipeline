source "https://rubygems.org"

ruby ">= 3.4.0"

# GitHub Pages gem handles Jekyll and all supported plugins
gem "github-pages", group: :jekyll_plugins

# Optional Ruby standard library gems (required for Ruby >= 3.4)
gem "csv"
gem "logger"
gem "base64"
gem "tzinfo"
gem "tzinfo-data"

# Windows file watching (optional but recommended)
gem "wdm", ">= 0.1.0", platforms: [:mingw, :x64_mingw, :mswin]
